# Agents

- Use semantic commits for all changes.
- Make all changes on feature branches.
- Create feature branches from `main`.
- Keep separate concerns on separate branches, even when one is noticed while
  working on another.
- Record every user-visible change under `## [Unreleased]` in `CHANGELOG.md`
  within the same pull request that makes the change.
